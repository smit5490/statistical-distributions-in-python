from setuptools import setup

setup(name='statdist',
      version='0.1',
      description='Basic statistical distributions in Python',
      packages=['statdist'],
      author='Robert Smith',
      author_email='rosmith731@yahoo.com',
      zip_safe=False,
      install_requires=['numpy', 'scipy', 'matplotlib'])
